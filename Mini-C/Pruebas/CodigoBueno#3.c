int var (int a, int b);
char* var2 (int a, int b, int*);
int* var (int a, int b, int*);
char var (int a, int b, int*);

int a, b, c=5;

int main (){
    int z,b,c,h=100, value;
    printf("%d", hola);
    if(a > b){
        int a = fac(a,b);
        a = fac(a,b,c);
	    
    }
    int a = a-b*8/7+5;
return value;
}



int* var (int a, int b, int*){
    int a,b,c,d=100;
    char value, formato ='c', acum;

    if(a > b){
        int a = fac(a,b);
        a = fac(a,b,c);
	    for(int i = 0 ; 15 > a ; i+=2){
		    while( h > 8){
                a--;
                printf("%d", hola);
	            printf("%c", hola);
	            scanf("%c", &b);
		    }
	    }
    }
char value, formato ='c', acum;
if(a > b){
        int a = fac(a,b);
        a = fac(a,b,c);
	    for(int i = 0 ; 15 > a ; i+=2){
		    while( h > 8){
                a--;
                printf("%d", hola);
	            printf("%c", hola);
	            scanf("%c", &b);
		    }
	    }
    }
int a = a-b*8/7+5;
return 'a';
}

int var (int a, int b){
int z,b,c,h=100;
char* k;
int* punter;
printf("%d", hola);
printf("%c", hola);
scanf("%c", &b);

if(a > b || h <= 100){
    int a = fac(a,b);
    a = fac(a,b,c);
	for(int i = 0 ; 15 > a ; i+=2){
		while( h > 8){
            a++;
            printf("%d", a);
            printf("%c", b);
		}
	}

    printf("Ingrese un numero: ");
    scanf("%d", &b);
}
return 100;
}
int var(int a, int b);
char *var2(int a, int b, int *);
int *var(int a, int b, int *);
char var(int a, int b, int *);

int a, b, c = 5;
char x, y, z;

int var(int a, int b)
{
    int z, b, c, h = 100;
    printf("Ingrese un numero: ");
    scanf("%d", &b);
    if ((a > b) && (i % 2 == 1))
    {
        int a = fac(a, b);
        fac(a, b, c);
        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8 || a < 100)
            {
                a *= 2;
                printf("Desea seguir calculando?");
                scanf("%c", &b);
            }
        }
    }
    int a = a-b*8/7+5;
    return a;
}

int main()
{
    int z, b, c, h = 100, value;
    printf("%d", hola);
    if (a > b)
    {
        int a = fac(a, b);
        a = fac(a, b, c);
        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a--;
                value = 8 * 5 + a * 4 * b - 10;
            }
        }
        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a--;
                value = 8 * 5 + a * 4 * b - 10;
            }
        }

        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a--;
                value = 8 * 5 + a * 4 * b - 10;
            }
        }

        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a--;
                value = 8 * 5 + a * 4 * b - 10;
            }
        }
    }
    return value;
}

int *var(int a, int b, int *)
{
    int a, b, c, d = 100;
    char value, formato = 'c', acum;
    int a = a-b*8/7+5;
    if (a > b)
    {
        int a = fac(a, b);
        a = fac(a, b, c);
        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a--;
                printf("%d", hola);
                printf("%c", hola);
                scanf("%c", &b);
            }
        }
    }
    return 'a';
}

int var(int a, int b)
{
    int z, b, c, h = 100;
    char *k;
    int *punter;
    printf("%d", hola);
    printf("%c", hola);
    scanf("%c", &b);

    if (a > b || h <= 100)
    {
        int a = fac(a, b);
        a = fac(a, b, c);
        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a++;
                printf("%d", a);
                printf("%c", a);
            }
        }

        printf("Ingrese un numero: ");
        scanf("%d", &b);
    }
    return 100;
}


int var(int a, int b)
{
    int z, b, c, h = 100;
    char *k;
    int *punter;
    printf("%d", hola);
    printf("%c", hola);
    scanf("%c", &b);

    if (a > b || h <= 100)
    {
        int a = fac(a, b);
        a = fac(a, b, c);
        for (int i = 0; 15 > a; i += 2)
        {
            while (h > 8)
            {
                a++;
                printf("%d", a);
                printf("%c", a);
            }
        }

        printf("Ingrese un numero: ");
        scanf("%d", &b);
    }
    return 100;
}