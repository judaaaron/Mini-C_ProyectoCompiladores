char* f(char a int*, int b, char i);
int f(char a int*, intx);
int* f(char a int*, int b);

int  funcion (int &a, char c, @){ 

while((i<a) || (5<4))
       int var = 6;
}

 for(int i=0; i<a; i+){
        printf("%c", id;
 }

scanf("c", id);

  return 0;

int  funcion (int &a, char c, @){ 

while((i<a) || (5<4))
       int var = 6;
}

 for(int i=0; i<a; i+){
        printf("%c", id;
 }

scanf("c", id);

  return 0;
}

int  funcion (int &a, char c, @){ 

while((i<a) || (5<4))
       int var = 6;
}

 for(int i=0; i<a; i+){
        printf("%c", id;
 }

scanf("c", id);

  return 0;
}
int  funcion (int &a, char c, @){ 

while((i<a) || (5<4))
       int var = 6;
}

 for(int i=0; i<a; i+){
        printf("%c", id;
 }

scanf("c", id);

  return 0;
}